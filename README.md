
# Desktop AI Pet

## 配置环境、运行启动与参数配置

项目程序位于 `desktop_pet/`。首次使用时，在该目录运行：

```powershell
.\setup_env.bat
```
`setup_env.bat` 会查找可用的 Python、创建项目专用的 `.desktop_pet_venv` 虚拟环境、安装 `requirements.txt` 中的依赖，并把实际运行解释器路径写入 `data/runtime_python.txt`。日常使用不需要重复执行；仅在首次配置、依赖损坏或更换 Python 环境后重新运行即可。

环境配置完成后，可双击运行：
```text
desktop_pet\start_main.vbs
```
`start_main.vbs` 会读取 `runtime_python.txt`、尽力执行一次 `git pull --ff-only` 更新当前分支，然后在后台启动 `main.py`。启动失败时会把原因写入 `desktop_pet/data/start_main_error.log`，并弹出错误查看窗口。

程序配置以 `desktop_pet/config/app_config.example.json` 为示例模板；如需保存本机配置，将其复制为同目录的 `app_config.json` 后修改。未创建 `app_config.json` 时，程序会直接使用示例配置。常用配置分组如下：

- `api`：聊天服务提供商、模型、API Key、超时和上下文长度限制；密钥仅填写在本机 `app_config.json`，不要提交。
- `ui`：人物缩放、置顶、右键菜单项和气泡展示时长。
- `behavior`：启动/主动问候、主动问候间隔、场景问候和每日限额。
- `chat`：正式问答模式与正式回答展示方式。
- `memory`：长期记忆、向量索引、Mem0 与嵌入服务开关。
- `reminders`、`screenshot`：提醒与截图解析功能的开关和限制。

##更新日志：
[v0.1.0] - 20260509
项目完成初始化，已能够稳定运行。
第一版功能如下：
1. 在 Windows 本机稳定启动。
2. 显示透明背景的人物。
3. 测试播放 idle、waving、running、failed、waiting、review 等基础动作。
4. 用户点击人物后能输入文字进行交流。
5. 成功调用 DeepSeek 官方 API 并显示回复。
6. 回复符合可爱、温柔、简短、自然的人格。
7. 启动时主动问候。
8. 空闲时静默显示主动气泡。
9. 用户不回复时，20 分钟内不会重复主动打扰。
10. 免打扰模式可关闭主动行为。
11. 聊天记录可保存为 JSON，并可以根据对话自动加载。
12. 上下文过长时可进行自动摘要。
13. API 失败、网络失败时程序不崩溃。
14. 记住上次退出位置,并在下次启动时自动加载。

[v0.1.1] - 20260509
新增功能：
1. 人物自主移动、跳跃功能。
2. 人物大小自定义缩放。
3. 退出前执行waving动作。
4. 增加正式回答模式。

[v0.1.2] - 20260510
新增功能：
1. 增加程序退出时的清理残留操作
2. 首次尝试移植程序，记录错误如下：
（1）安装完Python 3.12后使用错误的pip指令安装requirements.txt，从而使得解释器指向错误的旧解释器。
（2）后续移植时，使用py -m pip install -r requirements.txt绕过移植设备原本的python解释器。启动使用py main.py。

[v0.1.3] - 20260511
修复：
1. 修复位移测试。
新增：
2. 增加AGENTS.md，为智能体提供快速了解项目的概括文件。

[v0.1.4] - 20260511
新增功能：
1. 增加依赖时间的问候功能，包括季节性问候，时辰性问候。
2. 增加退出时的告别话术。
3. 增加双击问候功能。双击功能与人物主动问候关联，视为对人物问候的回应。
4. 增加窗口置顶的开启与关闭按钮，增加对应的回复。
6. 增加测试是否开启的配置。
7. 念诗。

[v0.1.5] - 20260511
修复：
1. 修复人物周围与回复气泡透明边框问题。
新增功能：
1. 增加聊天记录自动/手动地清理与总结功能，避免占用过大内存,逐步了解用户。

[v0.1.6] - 20260512
新增功能：
1. 增加分享知识的问候类型。知识问候与日常问候的比例，跟随用户回应而改变比例。
2. 增加针对未回应问候的时间间隔阶段性增长（15-15-30-30~60）。

[v1.0.0] - 20260512
1.0版本正式上线！

[v1.0.1] - 20260513
修复：
1. 取消开启问候计入每天问候次数。
2. 降低回应问候时，对应问候类型增加的比例。
3. 修复对话总计，记忆摘要的非空检查。

[v1.0.2] - 20260514
新增功能：
1. 新增自动配置环境脚本、快速启动脚本。

[v1.0.3] - 20260514
修复：
1. 修复气泡弹窗覆盖人物的问题。
新增功能：
1. 增加回复气泡与回答气泡互相感知的功能。

[v1.0.4] - 20260515
新增功能：
1. 新增开启时自动更新动作
修复：
1. 将环境配置从配置全局环境，变更为配置虚拟环境。确保迁移时用户计算机的纯净环境。

[v1.0.5] - 20260516
新增功能：
1. 增加重载配置按钮的开关选项。
2. 增加时段问候的下午问候。

[v1.0.6] - 20260517
修复：
1. 修复记忆总结时会错误融合人物输出的内容。

[v1.0.7] - 20260519
新增功能：
1. 增加气泡弹出时长的手动配置功能。

[v1.1.0] - 20260520
新增功能：
1. 增加mem0长期记忆的功能：
    （1）当前仅导入Mem0库，以及对应的使用程序。还未实际测试。
    （2）Mem0需要embedding模型，当前缺少对应的api。
    （3）未彻底丢弃先前的记忆功能。

[v1.1.1] - 20260521
新增功能：
1. 完整配置embedding模型，导入api，开发相应程序。

[v1.1.2] - 20260522
新增功能：
1. 增加一次性初始化问候。

[v1.1.3] - 20260525
1. 调整对话总结的触发条件。
2. 调整mem0调用与触发知识问候的先后顺序。
3. 调整对话总结、记忆记录。

[v1.2.0] - 20260529
1. 将清空聊天/总结独立出单独的线程。
2. 新增比例回调，避免比例改动只保存在内存而非本地。
3. 增加安全延迟关闭。

[v1.2.1] - 20260529
1. 精简三方库，减小内存占用。

[v1.2.2] - 20260530
1. 将部分后端处理功能与主线程隔离。
2. 为memory.json生成对应语义向量。
3. 增加语义级重复记忆合并的功能。

[v1.2.3] - 20260608
1. 新增 .tmp 清理，.bak 备份
2. 增加损坏主文件 .corrupt.<timestamp> 隔离、.bak 恢复
3. 增加flush + os.fsync + os.replace 原子保存

[v1.2.4] - 20260608
1. 新增 log_sanitizer.py (line 11)。
2. 提供 API key 脱敏、Bearer/api_key= 形态清理。
3. 增加长文本截断、异常摘要、messages 统计和响应结构摘要。
4. DeepSeekClient、Summarizer、Mem0MemoryService 的日志已改为记录结构信息和截断异常，不再输出完整 payload、prompt、memory、模型回复或 API response。

[v1.2.5] - 20260609
1. memory_vector_store.py 改为紧凑 JSON 保存：separators=(",", ":")，不再缩进。
2. embedding 写入前按配置精度压缩，默认 6 位小数。
3. 跳过短文本：配置缺失时内部默认少于 3 字符跳过；app_config.example.json 模板中设置。
4. 增加最大条目限制，默认 300，超限时优先保留关系记忆、当前项目等更重要字段和较新的条目。
5. embedding_signature 纳入精度配置，模型、维度、编码格式或精度变化会触发旧索引重建。

[v1.2.6] - 20260609
1. 后台任务退出与线程管理优化，新增一个轻量的后台任务管理模块。
2. 抽出窗口位置服务。不改动聊天、气泡、主动问候、动画和菜单逻辑。

[v1.2.7] - 20260609
1. 抽出气泡位置计算服务。不改变气泡样式、显示时机、聊天流程和主动问候流程。

[v1.2.8] - 20260609
1. 抽出后台任务注册管理。只抽出后台 QThread/worker 的注册、清理和退出管理逻辑。

[v1.2.9] - 20260611
1. 抽出配置读取辅助服务。只抽出配置读取与默认值处理的辅助逻辑，不改变配置文件格式和业务行为。

[v1.2.10] - 20260611
1. 轻量抽出聊天流程协调器

[v1.3.0] - 20260623
1. 优化角色设定系统，加入身份、人格、表达模式与场景反应。
2. 优化agent.md文件，强调中文编码问题。
3. 增加项目中文注释。

[v1.3.1] - 20260625
1. 修复退出线程强杀、修复JSON backup 恢复、摘要/聊天存储并发

[v1.3.1] - 20260710
1. 增加人物提醒功能，将提醒设置为可调用工具，当模型检测到需要调用时，返回标准格式。
2. 增加读取剪切版功能，相应增加剪切板润色、翻译等自动化功能。

[v1.3.2] - 20260711
1. 优化人物与用户交流的时间流。
2. 排查部分动作冲突的问题。
3. 去除.bak文件备份。

[v1.3.3] - 20260711
1. 增加截图识别功能.
2. 修改对话总结、记忆总结机制，增加定时总结与记忆覆盖+记忆时间功能。
3. 重构记忆存储的方式。

[v2.0.0] - 20260711
2.0正式上线！

[v2.0.1] - 20260714
1. 修改右键菜单显示方式，修改为卡片式弹窗。
